[pytest]
pythonpath = selenium_tests
markers =
    smoke: fast critical-path tests, run on every commit
    regression: full coverage, run before releases
    negative: error paths and invalid inputs
addopts = --html=reports/report.html --self-contained-html --alluredir=allure-results